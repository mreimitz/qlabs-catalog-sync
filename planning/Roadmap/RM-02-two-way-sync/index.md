# Two-way sync with conflict resolution

## Concepts

* [Two-way sync with conflict resolution](item.md) - Extend the bridge to reconcile and propagate metadata changes in both directions with a deterministic conflict-resolution policy.
