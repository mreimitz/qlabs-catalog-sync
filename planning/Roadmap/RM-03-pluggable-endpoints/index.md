# Pluggable endpoint framework for future catalogs

## Concepts

* [Pluggable endpoint framework for future catalogs](item.md) - Generalize the endpoint interface so new catalogs (Snowflake data catalog, Collibra, others) can be added without changing the core sync engine.
