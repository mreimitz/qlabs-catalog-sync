# Research Outputs

## Concepts

* [Connector Plugin SDK — Design Specification (v1)](connector-sdk-spec.md) - A Python SDK and entry-point plugin framework that makes each catalog an independently packaged, versioned, auto-discovered connector, so new endpoints ship without core changes.
