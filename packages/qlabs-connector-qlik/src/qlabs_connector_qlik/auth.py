"""Qlik auth + endpoint config.

WP3 / T3.1 (Sonnet). OAuth2 machine-to-machine auth and HttpEndpoint config
(tenant base URL), built on the SDK http (T1.4) and auth (T1.5) helpers.

TODO(T3.1): implement Qlik OAuth2 M2M auth and endpoint config.
"""
