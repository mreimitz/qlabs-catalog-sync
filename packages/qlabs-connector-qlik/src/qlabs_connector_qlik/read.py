"""Qlik read path.

WP3 / T3.3 (Sonnet). Reads Qlik entities into neutral envelopes so the engine can
diff desired state against existing target state before writing.

TODO(T3.3): implement read() Qlik entities -> neutral envelopes.
"""
