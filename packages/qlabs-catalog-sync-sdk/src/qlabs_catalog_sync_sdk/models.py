"""Neutral metadata model types (pydantic v2).

WP1 / T1.1 (Opus, foundational). Defines the vendor-neutral entities every connector
maps to and from: DataProduct, Dataset, GlossaryTerm, Category, Party, Tag,
FieldEnvelope, IdentityRef, FieldDiff. This is the contract's data shape; see the
RS-03 neutral metadata model specification.

TODO(T1.1): define the pydantic v2 models listed above.
"""
