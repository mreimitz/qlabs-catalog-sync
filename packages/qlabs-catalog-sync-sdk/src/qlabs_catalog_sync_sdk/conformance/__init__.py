"""Conformance test kit.

WP1 / T1.8 (Sonnet). A reusable pytest suite every connector runs to be "certified":
contract, round-trip, idempotency, HTTP behavior, and capability-honesty checks,
with a respx/vcrpy harness. Depends on the manifest (T1.3) and envelope (T1.6).

TODO(T1.8): implement the reusable conformance suite and harness.
"""
