"""Databricks auth + client config.

WP4 / T4.1 (Sonnet). OAuth machine-to-machine auth and client config via the
``databricks-sdk``, built on the SDK http/auth helpers.

TODO(T4.1): implement Databricks OAuth M2M auth and client config.
"""
