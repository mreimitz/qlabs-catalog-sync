"""Command-line entry point.

WP2 (Sonnet). CLI for running the service and one-off cycles, including the dry-run
mode (T2.8) that computes and logs planned writes without applying them.

TODO(T2.8): implement the CLI, including dry-run.
"""


def main() -> None:
    """Console-script entry point (placeholder).

    TODO(T2.8): parse args and run the service / a one-off cycle (with --dry-run).
    """
    raise NotImplementedError("qlabs-catalog-sync CLI is not implemented yet (WP2/T2.8)")

