"""Identity resolution.

WP7 / T7.1 (Opus). The IdentityMap store plus bootstrap matching by natural key
(name + type + parent path) with a confirmation workflow. Owners/parties are
best-effort metadata only and are explicitly not an identity system (see decision).

TODO(T7.1): implement the IdentityMap store and bootstrap matching.
"""
