---
description: Show a status overview of every research (RS) and roadmap (RM) item, grouped by status.
---

Produce a concise status board for this project. Do NOT create files — answer in chat.

1. Scan `Research/` for every `RS-NN-*` folder (ignore `RS-00-template`). For each, read its
   `README.md` and pull: tag, title, and status field.
2. Scan `Roadmap/` for every `RM-NN-*` folder (ignore `RM-00-template`) and `ROADMAP.md` for status.
3. Present a compact table grouped by status (active / blocked / planned / done / archived),
   showing tag → title, and note any cross-links (which RS feeds which RM, what's blocking what).
4. End with a one-line read on what looks stalled or unlinked, if anything.
